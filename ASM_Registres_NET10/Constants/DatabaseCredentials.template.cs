// TEMPLATE FILE — COPY to DatabaseCredentials.cs AND FILL IN REAL VALUES BEFORE BUILDING.
// The real file (DatabaseCredentials.cs) is excluded from git via .gitignore and must not be committed.
//
// Usage:
//   1. Copy this file next to it as "DatabaseCredentials.cs"
//   2. Rename the class if needed (the real one is `internal class DatabaseCredentials`)
//   3. Replace <HOST>, <USER>, <PASSWORD>, <DB> with the actual values
//
// NOTE: Do NOT delete this template.

/*
namespace ASM_Registres_NET10.Constants
{
    internal class DatabaseCredentials
    {
        public const string connectionString = "Host=<HOST>;Username=<USER>;Password=<PASSWORD>;Database=<DB>";
        public const string ConnectionStringLocal = "Host=<HOST>:<PORT>;Username=<USER>;Password=<PASSWORD>;Database=<DB>";
        public const string ConnectionStringAWS = "Host=<HOST>;Database=<DB>;Username=<USER>;Password='<PASSWORD>';Port=<PORT>";

        public const int ADMIN_LVL = 6;
        public const int USER_LVL = 1;
    }
}
*/
